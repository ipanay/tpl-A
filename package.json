{
  "name": "{{ name }}",
  "version": "0.0.1",
  "description": "{{ description }}",
  "main": "{{ main }}",
  "dependencies": {
    "bootstrap": "^4.4.1",
    "express": "^4.17.1",
    "vue": "^2.6.11"
  },
  "devDependencies": {
    "@vue/cli": "^4.2.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "{{ keywords }}"
  ],
  "author": "{{ author }}",
  "license": "ISC"
}
